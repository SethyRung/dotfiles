# CLI is `dotfiles`, not `dot`

dmmulroy's command is `dot`, but `dot` already exists on this PATH, so the user-facing command is `dotfiles` (`dotfiles init`, `dotfiles doctor`, `dotfiles stow`). Same CLI shape; different name to avoid the collision.
