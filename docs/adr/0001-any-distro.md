# Bootstrap targets any Linux distro

The only current machine is Ubuntu 26.04, and Ubuntu-only would ship faster. We still decided Bootstrap must work on any Linux distro (detect apt/pacman/dnf/etc.) so a Fresh Install is not locked to Ubuntu. Extra branching is accepted as the cost of not rewriting the Installer on the next distro.
