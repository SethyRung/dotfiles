# Repo lives at ~/Documents/projects/personal/dotfiles

Not `~/.dotfiles`. We keep this workspace path so development and Fresh Install use the same location. Clone on a Fresh Install must create `~/Documents/projects/personal/` first. Stow and the `dotfiles` stub are rooted here, not in `$HOME/.dotfiles`.
